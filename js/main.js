//Add buttonconst getAddBtn = document.querySelector('.add-button');const getClearBtn = document.getElementById('clear');const clearAll = document.querySelector('.main-container');getClearBtn.addEventListener('click', ()=>{    clearAll.style.display = "none";    const clearEveryThing = document.getElementById('task');    while (clearEveryThing.hasChildNodes()) {        clearEveryThing.removeChild(clearEveryThing.firstChild);    }});getAddBtn.addEventListener('click', ()=>{    //grab the value of the input field    const getInputValue = document.getElementById('new-task').value;    const math = Math.floor((Math.random() * 100) + 1);    clearAll.style.display = "block";    //Task Module    const buildTaskModule = (value, number) => {        const createTaskList = document.createElement('li');        const idNumber = number;        createTaskList.innerHTML = `        <i class="fa-solid fa-circle-xmark"></i>        <input id="box${idNumber}" name="box${idNumber}" class="checkbox" type="checkbox">        <label id="${idNumber}" for="${idNumber}" >${value}</label>        `;        //empty value field        document.getElementById('task').appendChild(createTaskList);        document.getElementById('new-task').value = '';    };    buildTaskModule(getInputValue, math);    //delete item    const deleteBtn =()=>{        const getDeleteBtn = document.querySelectorAll('.fa-solid');        console.log(getDeleteBtn);        getDeleteBtn.forEach(btn=>{            btn.addEventListener('click',(e)=>{                console.log('i have been clicked');                let selectedBtn = e.target;                if(e.target.click) {                    selectedBtn.parentElement.style.display = "none";                }            })        })    };    deleteBtn();    //select checkBox    const getCheckBox = () => {            const checkbox = document.querySelectorAll('.checkbox');            checkbox.forEach(item =>{                item.addEventListener('change',(e)=>{                    let selectedTask = e.target;                    if(e.target.checked) {                        selectedTask.nextElementSibling.classList.add('strike');                    }else{                        selectedTask.nextElementSibling.classList.remove('strike');                    }                })            })    };    getCheckBox(math);});